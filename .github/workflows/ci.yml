name: ci-rest-api

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

env:
  CARGO_TERM_COLOR: always

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: navigate to rest-api
      run: cd rest-api
    - name: build rest-api
      run: cargo build --verbose
    - name: run rest-api tests
      run: cargo test --verbose
